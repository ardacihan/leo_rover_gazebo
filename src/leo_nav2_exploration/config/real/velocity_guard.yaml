velocity_guard:
  ros__parameters:
    use_sim_time: false
    input_topic: /cmd_vel_smoothed
    output_topic: /cmd_vel_guarded
    scan_topic: /scan_filtered
    odom_topic: /wheel_odom
    battery_topic: /battery_state
    require_battery: false
    require_camera: false
    max_linear_speed: 0.26
    max_angular_speed: 0.9
    command_timeout: 1.5
    # Rover 4's firmware serial link delivers odometry in bursts under DDS
    # load (documented starvation); ride out 1-2 s gaps instead of zeroing.
    scan_timeout: 1.5
    odom_timeout: 2.5
    battery_timeout: 2.0
    min_battery_voltage: 11.0
    minimum_valid_scan_points: 30
    publish_rate: 20.0
    diagnostic_rate: 1.0
