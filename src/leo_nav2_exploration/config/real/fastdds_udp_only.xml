<?xml version="1.0" encoding="UTF-8" ?>
<!-- FastDDS UDP-only participant. Builtin transports include shared
     memory; wedged /dev/shm/fastrtps_* segments drop the Leo firmware
     endpoint (jetson-4 2026-08-20, same failure mode on jetson-02).
     Set FASTRTPS_DEFAULT_PROFILES_FILE to this file BEFORE rclpy.init().
     Do not use this profile on the Nav2 domain: ROS_LOCALHOST_ONLY=1
     already keeps costmaps off Ethernet, and a custom profile would
     replace that localhost-only default. -->
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">
  <transport_descriptors>
    <transport_descriptor>
      <transport_id>udp_transport</transport_id>
      <type>UDPv4</type>
    </transport_descriptor>
  </transport_descriptors>
  <participant profile_name="udp_only_participant" is_default_profile="true">
    <rtps>
      <userTransports>
        <transport_id>udp_transport</transport_id>
      </userTransports>
      <useBuiltinTransports>false</useBuiltinTransports>
    </rtps>
  </participant>
</profiles>
